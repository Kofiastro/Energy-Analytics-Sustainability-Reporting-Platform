# Source Code

The `src/` directory contains helper scripts used by the notebooks and pipelines.

- `src/load_energy_data.py` — Utilities to load and prepare datasets for analysis.

Usage example (from project root):

```bash
python "Energy Analytics & Sustainability Reporting Platform/src/load_energy_data.py" --help
```
